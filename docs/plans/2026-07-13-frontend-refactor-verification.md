# 页面重构验收记录

## 已完成范围

- 工作台外壳：桌面 64px 顶栏、手机 56px 品牌栏和 48px 固定页签栏。
- 室内编辑器：连续准备面板、手机收起步骤、稳定结果区、图片下方操作、缩略图切换、4 条历史折叠。
- 家具图册：紧凑上传工具、逐张处理批量上传、成功与失败统计、失败项重试、44px 删除按钮。
- 会员与邀请：平面会员状态、兑换和邀请区；邀请链接仍在会员中心内。
- 登录注册：桌面 44:56 分栏，使用合成前后示例；手机只保留品牌说明和表单。
- 管理后台：各数据块独立失败提示、统一状态色、手机横向导航、危险重置确认。

## 浏览器检查

登录页在以下尺寸完成实际渲染检查：

- [320px](../screenshots/2026-07-13-refactor/signin-320.png)：页面宽度与视口同为 320px，无横向滚动；宣传图不渲染。
- [768px](../screenshots/2026-07-13-refactor/signin-768.png)：页面宽度与视口同为 768px，表单保持单栏。
- [1440px](../screenshots/2026-07-13-refactor/signin-1440.png)：页面宽度与视口同为 1440px，44:56 分栏和前后对比图正常。

工作台与后台需要有效登录会话。本次没有创建测试账号或写入线上数据，因此没有伪造受保护页面截图。相关响应式结构、键盘名称、44px 操作区和局部失败状态由源码守卫测试覆盖；上线前仍建议用测试账号补拍图册、编辑器、会员和三张后台页面的三档截图。

## 合成素材

- 文件：`public/images/auth-room-before-after.png`
- 生成方式：内置图片生成工具。
- 提示词摘要：同一现代公寓客厅、相同镜头与自然光，左侧为空房，右侧放入奶油色沙发、胡桃木茶几、休闲椅、落地灯和绿植；写实室内摄影，无人物、文字、标志和客户数据。

## 命令验证

- `npm test`：191 项通过，0 项失败。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，24 个静态页面全部生成。
- `git diff --check`：通过。
- `npm run dev`：清理 `.next` / `.next-dev` 后正常启动，`/signin` 返回 200。

## 仍需人工验收

- 使用带家具、12 条以上历史和竖版房间图的测试账号检查真实数据密度。
- 实际触发上传部分失败、生成失败、次数限制、会员过期和历史加载更多失败。
- 用管理员测试账号确认邀请重置弹窗的焦点回收和各后台数据块的真实错误内容。
